# 实时单图换脸项目总文档

更新时间：2026-08-17

## 一句话目标

本项目只做一个核心目标：**实时单图换脸**。

当前不做 DFM 训练，不依赖 DeepFaceLive，不绕授权，不复制私有 Pro 编译模块。目标是在无授权限制的开源 Deep-Live-Cam 基础上，把单张授权源脸图片实时换到摄像头画面里，再稳定输出给 WhatsApp。

## 当前唯一主目录

```text
D:\DeepLiveCam2.7-Pro-0730_OSS_READY
```

旧目录、旧 DeepFaceLive、旧授权版目录只作为历史参考，不作为当前主方案。

## 当前唯一启动入口

用户只需要看根目录这 4 个文件：

```text
1_START_LOW_LATENCY.cmd  启动当前正式低延迟方案
2_STOP.cmd               停止本项目相关进程
3_SHOW_RECENT_HEALTH.cmd 查看最近健康日志
4_CAPTURE_STABILITY_SNAPSHOT.cmd 生成完整稳定性诊断快照
```

不要再使用旧的中文 bat、DeepFaceLive 入口、单独 OBS 入口或 Pro 授权版入口。

当前阶段只保留低延迟正式入口。质量增强、XSeg、边缘贴合进入后续单变量测试，不在日常启动时默认开启。

如果要判断一次视频测试到底是哪一段出问题，优先用 `4_CAPTURE_STABILITY_SNAPSHOT.cmd`，不要只看屏幕感觉。

## 当前最终链路

```text
DeepLiveCam
  -> Live / 直播
  -> OBS Output 窗口
  -> OBS 捕获 DeepLiveCam OBS Output
  -> DroidCam Virtual Output 插件
  -> Windows 摄像头设备 DroidCam Video
  -> WhatsApp Desktop 选择 DroidCam Video
```

关键结论：

- WhatsApp 不再直接选择 `OBS Virtual Camera`。
- WhatsApp 必须选择 `DroidCam Video`。
- OBS 仍然保留，用来捕获、缩放、锐化、调色和输出给 DroidCam。
- DeepLiveCam 自己的虚拟摄像头输出已禁用，避免和 OBS/DroidCam 抢设备。

## 当前默认启动配置

启动脚本：`tools\start_deeplivecam_obs.ps1`

当前默认源脸：

```text
source_faces\saokai11.jpg
```

当前 DeepLiveCam 参数：

```text
--source source_faces\saokai11.jpg
--execution-provider cuda
--execution-threads 2
--frame-processor face_swapper_hyperswap
--live-resizable
--live-fps-debug
--live-health-log
--similar-face-distance 1.5
--live-face-smooth 0.25
--live-face-fit-scale 1.06
--quality-preset low_latency
--no-live-xseg-mask
--live-obs-output-window
--no-live-virtualcam-output
--auto-live
-l zh
```

含义：

- `face_swapper_hyperswap`：当前默认使用 256 单图换脸模型路线。
- `low_latency`：当前正式低延迟预设。
- `--no-live-xseg-mask`：日常默认关闭 XSeg，先稳定帧率。
- `--live-face-smooth 0.25`：降低实时抖动，同时减少动态拖影。
- `--live-face-fit-scale 1.06`：让贴回区域略放大，减少目标脸比源脸大时露底。
- `--no-live-virtualcam-output`：不让 DeepLiveCam 直接输出虚拟摄像头，统一走 OBS。

## 当前测试模式

当前正式模式：

```text
入口：1_START_LOW_LATENCY.cmd
HyperSwap 256
XSeg 关
quality preset：low_latency
Face Enhancer：关
```

判断规则：

```text
当前阶段先看帧率稳定性。
如果帧率稳定但边缘变差，再进入 XSeg / mask / face_fit_scale 单变量测试。
如果帧率不稳，先通过健康日志定位检测、换脸、增强、OBS/DroidCam 哪一段拖慢。
```

## 当前 OBS / WhatsApp 配置

OBS：

```text
场景：DeepLiveCam
源：DeepLiveCam OBS Output
捕获窗口：OBS Output:HighGUI class:python.exe
画布：1280x720
FPS：30
```

WhatsApp：

```text
摄像头：DroidCam Video
不要选：OBS Virtual Camera
```

新设备复发风险：

```text
如果 WhatsApp 能看到 DroidCam Video，但显示 Start DroidCam、黑屏或没有换脸画面，优先怀疑 OBS 到 DroidCam 的输出没有真正 active。
这不是主换脸模型问题，也不是图片问题。
```

NEXT 已固定：

```text
OBS profile：DeepLiveCam
OBS profile dir：DeepLiveCam
OBS scene collection：DeepLiveCam
DroidCamVirtualOutput AutoStart=true
OBS output：1280x720 / 30fps
OBS source：OBS Output:HighGUI class:python.exe
```

诊断入口：

```text
5_DIAGNOSE_WHATSAPP_VIDEO.cmd
```

它会生成：

```text
logs\whatsapp_video_chain_YYYYMMDD_HHMMSS.txt
```

判断方式：

```text
OBS 预览黑屏：OBS window capture 没锁到 OBS Output。
OBS 有画面但 WhatsApp 黑屏或 Start DroidCam：DroidCam Virtual Output 没 active、OBS profile 错、或 WhatsApp 读到旧占位流。
OBS 日志没有 droidcam-virtual-output.dll：插件没加载或便携 OBS 路径不对。
OBS 日志没有 webcam video active：WhatsApp 没有真正读取 DroidCam 流，或 DroidCam 输出没启动。
WhatsApp 没有 DroidCam Video 选项：驱动没装好或需要重启系统。
```

标准启动顺序：

```text
1. 先运行 2_STOP.cmd。
2. 再运行 1_START_LOW_LATENCY.cmd。
3. 等 OBS Output 窗口出现且有换脸画面。
4. 确认 OBS 预览有该画面。
5. 再打开 WhatsApp 并选择 DroidCam Video。
6. 如果无画面，运行 5_DIAGNOSE_WHATSAPP_VIDEO.cmd。
```

当前 WhatsApp 安全比例：

```text
OBS 画布：1280x720
源画面区域：970x546
位置 X：155
位置 Y：86
用途：当前稳定回退值
```

如果画面仍然太大：

```text
源画面区域：900x506
位置 X：190
位置 Y：107
用途：只作为横屏内缩小构图候选
注意：不要把大小调成 506x900 这种竖向尺寸。当前 WhatsApp/DroidCam 稳定链路是 1280x720 横屏流，竖向尺寸会导致构图异常、黑边或画面观感变差。
```

## 摄像头策略

当前同意并固定的方向：

```text
摄像头输入：1280x720 / 30fps，MJPG
OBS 输出：先保持 1280x720 / 30fps
WhatsApp：走 DroidCam Video
不建议默认用 4K
```

原因：

- WhatsApp 最终会压缩，4K 不会让对方看到真正 4K。
- 4K 会增加 DeepLiveCam、OBS、DroidCam、WhatsApp 的整体压力。
- 当前项目优先真实、稳定、低延迟，1080p 输入比 4K 更合理。
- 如果 4K 导致掉帧，最终观感会比干净的 1080p 更差。

## 当前已经做到什么

### 已经跑通

- 开源无授权限制项目作为主方案。
- DeepLiveCam 实时摄像头换脸。
- OBS 捕获最终换脸输出窗口，不捕获 cmd，不捕获原始摄像头。
- DroidCam Video 被 WhatsApp Desktop 正常识别。
- WhatsApp 视频通话能选择 DroidCam Video 并显示画面。
- OBS Virtual Camera 与 WhatsApp 不兼容的问题已绕开。

### 已经接入的质量模块

```text
HyperSwap 256
XSeg mask
Poisson Blend
mouth mask
单人目标追踪
bbox/keypoint 平滑
face fit scale
GPEN-256 / GPEN-512 可选
GFPGAN 可选
OBS 锐化 / 颜色稳定
```

### 当前模型文件

```text
inswapper_128.onnx
inswapper_128_fp16.onnx
hyperswap_1a_256.onnx
hyperswap_1b_256.onnx
hyperswap_1c_256.onnx
GPEN-BFR-256.onnx
GPEN-BFR-512.onnx
GFPGANv1.4.onnx
GFPGANv1.4.fp16.onnx
gfpgan-1024.onnx
xseg.onnx
xseg_cuda_v1.onnx
insightface\buffalo_l
```

## 当前还缺什么

这些是“真实感”差距，不是简单 FPS 问题：

1. **更强的遮挡保护**
   手、眼镜、头发、麦克风经过脸部时，当前 XSeg 只能减轻穿帮，不能保证完全保护。

2. **更强的脸型约束**
   如果源脸和目标脸脸宽、下巴、额头比例差太多，当前只能靠 `face fit scale` 和 mask 缓解，不能彻底改变脸型结构。

3. **更强的动态跟踪**
   快速转头、低头、侧脸时，当前平滑能减少抖动，但无法保证每帧都完美贴合。

4. **更智能的局部保护**
   眼睛、牙齿、嘴唇、下巴边缘还没有足够精细的独立保护策略。

5. **更强的生成模型**
   HyperSwap 256 比 inswapper_128 上限更高，但仍是单图实时方案，不等于训练人物专属模型。

6. **更自然的嘴部动态**
   单图实时换脸容易让嘴唇颜色、嘴形和牙齿区域显得僵硬。该问题排在脸部边缘优化之后处理，优先测试 mouth mask / mouth region protection，而不是直接叠加高清增强。

## 下一阶段总方案

当前不要把三个问题拆开乱调。它们都属于“最终通话真实度和稳定性”这一个大问题，但要分层处理：

```text
第一层：断线/掉帧/掉脸日志
第二层：脸部边缘贴合
第三层：嘴唇动态自然度
```

### 第一层：断线/掉帧/掉脸日志

目标不是马上自动挂断，也不是马上改参数，而是先判断问题发生在哪一段：

```text
DeepLiveCam 本地处理
摄像头输入
OBS 窗口捕获
DroidCam 虚拟输出
WhatsApp 读取
VPN / 网络传输
```

新增入口：

```text
4_CAPTURE_STABILITY_SNAPSHOT.cmd
```

生成文件：

```text
logs\stability_snapshot_YYYYMMDD_HHMMSS.txt
```

它记录：

- DeepLiveCam 的 `process_fps / camera_fps / miss_rate / queue_in / queue_out`。
- 新日志可记录 `avg_ms_detect / avg_ms_swap / avg_ms_enhance / avg_ms_other`。
- OBS 日志里的 DroidCam active / inactive / output stopping。
- Windows 当前识别到的 DroidCam Video、OBS、4k Camera 设备。
- WhatsApp 进程和连接状态。
- 网络延迟、最大延迟、丢包快速样本。

判断标准：

```text
process_fps 平均低于 25：本地处理或模型负担问题。
camera_fps 平均低于 25：摄像头输入规格或驱动问题。
miss_rate 高于 5%：掉脸、遮挡、侧脸、脸离太近或光线问题。
max_consecutive_missed 大于 2：连续掉脸，后续才考虑保护策略。
queue_in / queue_out 长期大于 1：处理队列堆积。
avg_ms_enhance 明显大于 0：说明增强器正在增加延迟。
OBS 出现 DroidCam inactive/stopping：OBS 到 DroidCam 链路断流。
网络 max latency 高或丢包：WhatsApp 会主动降码率、卡顿、糊。
```

当前策略：

```text
只记录，不自动断通话。
自动挂断必须等多轮日志确认误判率很低之后再做。
```

### 掉脸/遮挡保护的优化原则

参考方向：

```text
Deep-Live-Cam 2.7 Ultimate：强调 async face detection、smart caching、mask/render 优化、lip-mask 修复、Poisson + feathering、颜色匹配、详细 landmarks。
FaceFusion：提供 box / occlusion / region 多种 face mask，并支持 blur、padding、mouth、upper-lip、lower-lip、glasses 等区域级 mask。
iRoopDeepFaceCam：强调 mouth mask 和 face tracking，说明初始清晰无遮挡的人脸对后续追踪很重要；遮挡太久会丢 track。
FaceShifter 论文：高质量方案通常把遮挡作为独立问题处理，使用专门的 occlusion-aware refinement，而不是只靠边缘模糊。
```

来源记录：

```text
https://github.com/hacksider/Deep-Live-Cam/releases
https://github.com/player29879/facefusion
https://github.com/iVideoGameBoss/iRoopDeepFaceCam
https://arxiv.org/abs/1912.13457
```

对当前项目的结论：

```text
不能每帧都额外跑重型真实度检测模型。
实时链路里最稳的做法是：轻量状态检测 + 追踪缓存 + 阈值门控 + 必要时冻结/降级输出。
重型遮挡解析、XSeg、增强器只在质量测试或高性能机器上逐步打开。
```

要区分三种状态：

```text
1. no_face
   人真的离开摄像头或脸完全不在画面。
   处理：记录即可，不当成软件错误。

2. occluded_or_bad_pose
   手遮挡、低头、侧脸、大幅转头、脸太近、脸到画面边缘。
   处理：不要硬换脸；后续可测试冻结上一帧、降低换脸强度、切回原画或人工提示。

3. false_miss
   正脸清楚在画面里，但检测失败。
   处理：这是检测/追踪问题，需要调 detector threshold、检测频率、bbox 平滑、目标脸锁定和重检测策略。
```

实时判断不直接看“换脸像不像”，而先看低成本信号：

```text
是否检测到脸
人脸框面积是否突然变化
人脸框中心是否突然跳动
关键点置信度是否异常
连续 miss 帧数
上一帧和当前帧的 bbox/landmark 距离
process_fps 是否掉到阈值以下
queue 是否开始堆积
```

后续保护策略优先级：

```text
P0 记录状态：把 no_face / occluded_or_bad_pose / false_miss 分开写日志。
P1 单人目标锁定：只跟踪第一个稳定目标，避免误换到背景人脸或新进入的人脸。
P2 短时冻结：连续 miss 很短时保持上一帧位置/参数，避免画面闪烁。
P3 置信度门控：检测分数低或 bbox 跳太大时不强行更新换脸位置。
P4 遮挡质量模式：在新硬件上测试 XSeg/region mask，优先保护嘴、眼镜、头发、手部遮挡。
P5 自动断通话：最后才考虑，且必须基于多轮日志证明误判率低。
```

为什么现在先不做自动断通话：

```text
当前 miss_rate 已证明会被用户离开镜头、手遮挡触发。
这些不等于软件故障。
如果现在直接自动断通话，会把正常动作误判成事故。
```

脸部边缘优化的主战场：

```text
DeepLiveCam：负责贴合位置、脸大小、mask 范围、mask blur、XSeg、嘴部/眼部区域保护。
OBS：只负责最终画面的轻微锐化、柔化、亮度、对比度、饱和度、裁切。
WhatsApp/DroidCam：负责远端接收和压缩，不负责修复脸是否贴住。
```

所以边缘不贴合时优先调 DeepLiveCam，不优先调 OBS。
OBS 只能让画面更均衡，不能把一张没有贴住的脸变成真实贴合。

### 最终效果优先的取舍方案

本项目追求的是 WhatsApp 远端看到的最终真实度，不是把每个技术点做到理论满分。
所以后续所有优化都按“真实感收益 / 性能成本 / 风险”排序。

#### 掉脸保护准备怎么实现

目标：

```text
不追求任何遮挡都强行换脸。
追求异常发生时不要突然闪、漂、穿模、露出很假的画面。
```

当前行业里效果最好的方向不是单一技巧，而是组合：

```text
高质量换脸模型
目标人脸属性保持
遮挡感知 refinement
区域级 face mask
时序追踪和缓存
颜色/边缘融合
```

论文路线如 FaceShifter / FaceDancer 会把遮挡、姿态、属性保持放进模型结构里，效果上限高，但它们不是我们现在这种 WhatsApp 实时单图工具可以直接无成本接入的方案。
开源实时工具更现实的路线，是 FaceFusion / Deep-Live-Cam 2.7 这种：

```text
轻量追踪
region / occlusion / mouth mask
lip / chin / eyes mask
feathering / blend
异步检测和缓存
```

适合我们的实现顺序：

```text
1. 状态分类日志
   no_face：人离开画面。
   occluded_or_bad_pose：手遮挡、低头、侧脸、脸太近。
   false_miss：正脸在画面里但检测失败。

2. 短时稳定保护
   只在 0.2-0.5 秒级别内使用上一帧 bbox / landmark / mask 参数。
   目的：防止轻微抖动导致脸闪。
   风险：冻结太久会假，所以必须很短。

3. 置信度门控
   当 bbox 面积突然变化、中心突然跳动、连续 miss 过多时，不立刻相信新检测结果。
   目的：避免脸跳到错误位置或背景人脸。

4. 降级输出策略
   短时遮挡：保留上一帧稳定参数。
   长时遮挡或离开画面：不硬换，允许原画面或安全画面。
   自动挂断：最后阶段再做，且必须经过多轮日志验证。

5. 质量模式遮挡保护
   新硬件上再测试 XSeg / occlusion mask / region mask。
   这会吃性能，不应该在低延迟模式里一上来全开。
```
如果网络断链是不是短期保护做好，其实断链时也就会保持短期保护的画面？所以只要短期文档保护就足够应对网络断链了？

不建议现在做的：

```text
每帧跑一个大模型判断“换脸是否真实”。
每帧跑重型遮挡分割。
一检测异常就自动挂断。
```

原因：

```text
它们会增加延迟，可能造成卡顿。
误判会比当前问题更烦。
视频通话里每帧稳定比偶尔局部更清晰更重要。
```
同意，性能必须要保证，FPS要稳定，不能再低，如果可以后期还要再往高的提升。
#### OBS 美化能做什么

OBS 应该担任“最终画面调色和轻微修饰”，不是担任“修脸模型”。

OBS 适合做：

```text
轻微锐化：提升 WhatsApp 压缩后的边缘观感。
轻微柔化：降低换脸区域和原画面的割裂感。
亮度/对比度：避免脸和背景亮度不一致。
饱和度：压住嘴唇过红、肤色过艳。
Gamma：调整暗部，让脸不灰不脏。
LUT：固定一套稳定色彩风格。
裁切/缩放：控制 WhatsApp 看到的人脸大小。
```

OBS 不适合做：

```text
修复脸型不贴合。
修复边缘穿模。
修复嘴唇死板。
修复侧脸掉脸。
把低质量换脸变成高质量换脸。
```

预计分工：

```text
DeepLiveCam：负责真实度核心，包括脸大小、位置、mask、边缘、嘴部区域。
OBS：负责整体观感，包括清晰、柔和、肤色、压缩前画面均衡。
WhatsApp/DroidCam：只负责传输和接收，不能指望它修图。
```

OBS 的最佳使用原则：

```text
宁可小幅度、多次验证。
不做强美颜。
不把脸磨到没有皮肤细节。
不让 OBS 掩盖 DeepLiveCam 的真实问题。
```

#### 嘴部覆盖降低会不会断层

会有这个风险。

嘴部区域有三个取舍：

```text
1. 源图嘴唇覆盖强
   优点：更像源图。
   缺点：说话死板、牙齿/口型不自然、像贴上去。

2. 保留目标嘴部动态更多
   优点：说话自然，口型和牙齿跟得上。
   缺点：源图身份相似度下降，嘴唇可能不像源图。

3. 过渡区做柔和融合
   优点：在相似度和动态之间找平衡。
   缺点：调不好会出现断层、糊圈、嘴角漂。
```
同意，2和3的结合。但是最终效果判断要有具体参数或者具体参照，让我能精确判断那个数值需要调整

所以嘴部优化不能简单理解成“降低覆盖就好”。
正确策略是：

```text
先保护牙齿和口腔运动。
再保留嘴唇外轮廓和颜色的一部分源图特征。
嘴角、上唇、下唇分区调，不一次性整体降低。
优先选择闭嘴或自然轻微微笑的源图，减少模型幻想。
```

验收标准：

```text
静止时嘴唇不能明显不像源图。
说话时嘴形不能像一张固定贴纸。
牙齿不能糊成红色或黑块。
嘴角不能和脸颊断开。
WhatsApp 远端看不能出现明显断层。
```

#### 当前最均衡路线

```text
第一步：保留当前低延迟稳定链路。
第二步：实现轻量掉脸状态分类和短时保护，不自动挂断。
第三步：单变量调脸部边缘，主调 DeepLiveCam，不先动 OBS。
第四步：OBS 只做轻微锐化、轻微柔化、色彩统一。
第五步：嘴部只做小范围 mouth mask 测试，避免大幅牺牲身份相似度。
第六步：每次以 WhatsApp 远端观感 + stability_snapshot 日志为准。
```

### 第二层：脸部边缘贴合

边缘不贴合不是一个按钮能解决，通常来自四个因素叠加：

```text
源脸脸型和目标脸型不匹配
face_fit_scale 没有匹配目标脸宽
mask 范围和 blur 不合适
XSeg/遮挡策略没有打开或强度不够
```

测试方式必须单变量：

```text
A. 当前基线不动，记录截图和日志。
B. 只改 face_fit_scale，例如 1.04 / 1.06 / 1.08 / 1.10。
C. 固定最佳 face_fit_scale 后，再调 mask scale / blur。
D. 固定前两项后，再打开 XSeg 做质量测试。
E. 最后才考虑 OBS 轻微锐化/柔化。
```

验收方式：

```text
正脸静止不能像贴纸。
轻微左右转头不能明显露出原脸边缘。
下巴、脸颊、太阳穴不能大面积糊边。
不能为了盖住边缘把整张脸糊成一片。
最终以 WhatsApp 对方看到的画面为准。
```

### 第三层：嘴唇动态自然度

嘴唇死板是单图实时换脸常见问题。核心原因通常不是 FPS，而是源图嘴唇纹理、单图模型表达能力和嘴部区域覆盖方式。

优化顺序：

```text
1. 先确认嘴唇问题是否只在说话时出现，静止是否正常。
2. 源图优先选择闭嘴或自然轻微微笑，避免大红唇、强妆、张嘴图。
3. 测试 mouth mask / mouth region protection，保留更多目标嘴部动态。
4. 降低嘴唇区域覆盖强度，让牙齿、嘴形运动更像真人。
5. 最后才考虑外部 lip-sync / expression-restorer。
```

取舍：

```text
保留目标嘴部动态：说话更自然，但身份相似度可能下降一点。
完全覆盖源图嘴唇：更像源图，但说话更容易僵硬。
外部 lip-sync：理论更强，但延迟、稳定性和链路复杂度都会上升。
```

### 你和我的分工

用户负责：

```text
判断对方 WhatsApp 看到的最终效果。
在出现异常时说明动作：静止、说话、转头、低头、遮挡、网络切换。
必要时提供不暴露隐私的截图或日志文本。
```

Codex 负责：

```text
维护启动脚本、日志脚本、文档和同步包。
读取日志并判断问题属于哪一段。
设计单变量测试顺序。
只有在会影响当前满意效果时，先明确提示风险，再改参数。
```

## 不要误解显卡升级

4080 / 4080S / 5070Ti 能解决的是：

- 更高 FPS。
- 更低延迟。
- 更稳定开启 XSeg、GPEN-256、GPEN-512。
- 减少因为性能不足导致的画面波动。
- 让高质量参数有实际测试空间。

显卡不能直接解决的是：

- 源脸图片不合适。
- 源脸和目标脸型差太大。
- 嘴唇颜色/妆容本身异常。
- 单图模型身份保持能力不足。
- WhatsApp 自身压缩导致对方画面变糊。

所以硬件升级后，测试重点不是“FPS 变高了没有”，而是看能否稳定开启高质量模块后，真实感是否明显改善。

## 单图源脸图片质量标准

这是本项目最重要的素材标准。源图不合格，配置再漂亮也会拉跨。
同意，换脸图片原素材非常重要

### A 级，可优先测试

- 正脸，左右转头小于 10 度。
- 低头/仰头很轻，眼睛、鼻子、嘴巴位置自然。
- 脸部宽度至少 300px，越大越好。
- 双眼之间距离至少 120px，越大越好。
- 眼睛、鼻翼、嘴唇边缘清晰。
- 光线均匀，不背光，不过曝，不一边脸黑一边脸亮。
- 没有头发遮眼、手遮脸、墨镜、口罩、强反光眼镜。
- 表情自然，闭嘴或轻微微笑。
- 皮肤和嘴唇颜色自然，没有重滤镜、紫色嘴唇、过度美颜。
- 原图或高质量图片，不是多次压缩后的聊天软件小图。
- 脸型和目标摄像头里的脸尽量接近，尤其是脸宽、下巴、额头比例。

### B 级，可以测试但要记录问题

- 轻微侧脸，左右转头 10-20 度。
- 有轻微美颜或轻微磨皮。
- 戴普通眼镜但无强反光。
- 光线略不均匀但五官清楚。
- 脸部够大但截图压缩较明显。

### C 级，不建议作为主测试图

- 大角度侧脸。
- 脸太小、截图糊、视频运动模糊。
- 强滤镜、强磨皮、AI 修复过度。
- 嘴唇颜色异常、妆容过重。
- 头发遮住眼睛或脸边缘。
- 表情夸张、张嘴、大笑。
- 低清、过曝、背光、阴影很重。

## 真实感验收标准

不要只看“清不清楚”，要按下面维度评分。

每项 1-5 分：

```text
1 = 无法使用
2 = 明显假
3 = 可测试但破绽明显
4 = 基本可用
5 = 接近真实
```

### 1. 身份相似度

看起来是否像源图人物。重点看眼型、鼻子、嘴唇、脸部整体气质。

### 2. 脸型贴合

看脸宽、下巴、额头、脸颊是否贴在目标脸上，不要像一张脸皮浮在上面。

### 3. 边缘融合

看额头、太阳穴、脸颊、下巴、耳侧是否露底、糊边、硬边。

### 4. 五官稳定

转头、眨眼、说话时眼睛、鼻子、嘴巴是否漂移。

### 5. 嘴部真实

说话时嘴唇颜色、嘴形、牙齿区域是否自然。

### 6. 遮挡表现

手、眼镜、头发靠近脸部时是否穿模。

### 7. 清晰度

看眼睛、鼻翼、嘴唇边缘，不只看皮肤是否光滑。

### 8. 颜色一致

源脸区域和摄像头原画面肤色是否一致，不偏紫、不偏灰、不像贴纸。

### 9. 动态稳定

正常说话、轻微转头 5-10 分钟，画面是否持续稳定。

### 10. 远端效果

WhatsApp 对方手机看到的画面是否仍可接受。最终以对方看到的画面为准。

## 测试流程

每次测试只改一个变量。不要同时换图、换模型、换 enhancer、换 OBS 分辨率，否则无法判断原因。

## 版本管理与优化边界

当前已经跑通的版本必须先留存，作为后续对比基准。

基准版本要求：

```text
WhatsApp 能看到 DroidCam Video 输出。
OBS 能捕获 OBS Output。
DeepLiveCam 能稳定输出换脸画面。
摄像头输入为 1280x720 / 30fps。
HyperSwap 256 可正常运行。
Face Enhancer 默认关闭。
XSeg 默认关闭。
```

后续新版本优化原则：

```text
只修改需要优化的部位。
已经合格的链路不轻易改。
每次只改一个变量。
涉及大问题或会影响已合格业务链路时，先停止，给出依据和风险，再决定是否执行。
不能为了小问题破坏主链路。
```

问题分级：

```text
大问题：
启动失败、摄像头打不开、OBS/DroidCam/WhatsApp 链路断、FPS 大幅下降、换脸完全失效。

中问题：
嘴巴死板、边缘虚、轻微穿模、颜色不统一、远端画面偏糊。

小问题：
局部锐度、轻微肤色、画面比例、轻微过曝或过暗。
```

下一版优先级：

```text
P0：保留当前稳定版本并打包标记。
P1：整理目录，旧包统一归档，新版放 D 盘根目录。
P2：优先解决嘴巴自然度和 OBS 观感。
P3：现有主换脸参数先保持不动。
P4：脸部边缘贴合进入下一阶段，由用户看效果后再确认。
```

判断标准：

```text
不是看本地预览单点效果，而是看 WhatsApp 远端最终效果。
不是只看清晰度，而是看真实感、稳定性、延迟和穿帮概率。
每次优化后都要能和基准版本对比。
```

### 第 1 步：固定环境

```text
摄像头：1920x1080 / 30fps
OBS：1280x720 / 30fps
WhatsApp：DroidCam Video
灯光：固定
坐姿和脸到摄像头距离：固定
```

### 第 2 步：源图测试

固定所有参数，只换源脸图片。

记录：

```text
源图文件名
图片等级：A/B/C
身份相似度
脸型贴合
边缘融合
嘴部真实
颜色一致
备注
```

### 第 3 步：模型/处理器测试

同一张源图下对比：

```text
HyperSwap 256
inswapper_128
HyperSwap 256 + XSeg
HyperSwap 256 + XSeg + GPEN-256
HyperSwap 256 + XSeg + GPEN-512
```

当前重点不是 FPS，而是看哪个组合最真实。

### 第 4 步：动态测试

每个候选配置必须做：

```text
正脸静止 30 秒
正常说话 1 分钟
左右轻微转头
低头/抬头
手靠近脸部
戴眼镜/不戴眼镜，如果实际会用
WhatsApp 对方手机观看
```

### 第 5 步：记录结论

只保留有价值的结论：

```text
哪张图最好
哪组参数最好
最大破绽是什么
这个破绽属于源图问题、模型问题、mask 问题、硬件性能问题，还是 WhatsApp 压缩问题
下一次只改什么
```

### 第 6 步：稳定性快照

每次 WhatsApp 实测结束后运行：

```text
4_CAPTURE_STABILITY_SNAPSHOT.cmd
```

把生成的 `logs\stability_snapshot_*.txt` 复制回来分析。

如果只是快速看最近健康日志，可以运行：

```text
3_SHOW_RECENT_HEALTH.cmd
```

## 当前优化方向排序

优先级从高到低：

1. 先做断线/掉帧/掉脸日志增强和多轮快照。
2. 严格筛选 A 级源图。
3. 固定 720p/30fps 摄像头输入、OBS 输出和稳定灯光。
4. 继续以 HyperSwap 256 为主模型测试。
5. 先完成脸部边缘贴合微调。
6. 再优化 mouth mask、眼部/嘴部局部保护，解决说话时嘴唇死板。
7. 在新硬件上重新测试 XSeg、GPEN-256、GPEN-512。
8. 对比 deeplivecam.net 官方新版，但先做同素材、同灯光、同 WhatsApp 输出对比。
9. 如果单图方案仍达不到目标，再重新讨论是否进入训练或更高阶模型路线。

## 当前不做的事

- 不训练 DFM。
- 不继续 DeepFaceLive 方案。
- 不绕授权。
- 不复制 Pro 私有模块。
- 不盲目改 4K。
- 不只看本地 OBS 预览，必须看 WhatsApp 远端实际效果。

## 外部方案对比原则

未来测试 `https://deeplivecam.net/index.php` 或官方新版时，必须按同一标准比较：

```text
同一张源脸图
同一台摄像头
同一光线
同一 OBS/DroidCam/WhatsApp 输出链路
同一动作测试
同一评分表
```

如果新版只是 FPS 高，但边缘、嘴部、脸型没有改善，不算成功。

如果新版让遮挡、脸型、嘴部、动态稳定明显更好，即使配置复杂，也值得迁移。

## 硬件采购结论暂定

当前偏向：

```text
优先：RTX 4080 Super / RTX 4080
备选：RTX 5070Ti
不建议长期主力：RTX 4070Ti 12GB
```

原因：

- 4080/4080S 的 16GB 显存够用，软件生态成熟，实时链路更稳。
- 5070Ti 有新架构潜力，但需要更严格匹配 CUDA / ONNX Runtime / PyTorch。
- 对当前目标来说，稳定开启高质量模块比纸面 AI TOPS 更重要。

最终采购前仍要看价格。如果 5070Ti 价格明显更好，也可以作为新平台方案，但要接受环境调试成本。

## 相关历史文档

这些文件保留为历史记录，但以后优先看本文档：

```text
WHATSAPP_DROIDCAM方案记录.zh-CN.md
实时换脸优化记录_2026-08-15.md
FEATURE_MIGRATION_PLAN.zh-CN.md
项目需求_配置_测试记录.md
使用说明.txt
FINAL_READINESS_REPORT.zh-CN.md
```

## 下一步

下一步不是继续盲目改参数，而是先把稳定性日志跑通，再建立一套素材和配置对照测试：

1. 用当前默认配置跑一次 WhatsApp 视频。
2. 测试结束运行 `4_CAPTURE_STABILITY_SNAPSHOT.cmd`。
3. 确认日志能判断本地、OBS/DroidCam、WhatsApp、网络四段状态。
4. 从授权素材里挑 5-10 张 A/B 级源脸图。
5. 每张图用当前默认配置跑同一套动作。
6. 按 10 项真实感标准打分。
7. 选出最接近真实的 1-2 张图。
8. 再在这 1-2 张图上测试 face_fit_scale、mask、XSeg、OBS 滤镜和新版软件。

只有这样，才能判断“是软件不行、模型不行、图不行，还是硬件不行”。

## 2026-09-01 当前调试基准

本节只记录当前实时单图换脸目标需要的信息：源图生成/评估、OBS 输出、DeepLiveCam 实时参数。

### AI 生成标准源脸图命令

用途：把已有授权照片作为参考，让 AI 生成更适合实时单图换脸的标准正脸源图。

给 AI 的命令：

```text
我会提供 3-8 张已授权的人脸参考照片。请基于这些参考照片生成一张适合实时单图换脸的真实照片级源脸图。

必须满足：
1. 保持同一人物身份特征，不要改年龄、人种、五官比例和脸型核心特征。
2. 正脸，头部水平，眼睛看向镜头，左右转头不超过 10 度。
3. 镜头与眼睛同高，不要俯拍、仰拍、广角畸变。
4. 面部完整，额头、下巴、左右脸颊轮廓完整可见，不裁切脸。
5. 无眼镜、无手遮挡、无头发遮挡眼睛和脸颊边缘。
6. 表情自然，闭嘴或轻微放松，嘴唇颜色自然，不要夸张口红。
7. 光线均匀柔和，脸部没有强阴影、强反光、过曝。
8. 皮肤保留真实纹理，不要重度美颜、磨皮、塑料感、CG 感。
9. 背景简单干净，不能有文字、水印、贴纸。
10. 输出比例 1:1，建议 1024x1024 或更高，真实相机人像风格。

请输出 3 个版本：
A. 身份相似优先。
B. 脸型贴合优先，脸宽和下巴更自然。
C. 换脸测试优先，正脸、轮廓、光线最标准。

不要输出漫画、写真大片、过度美颜、网红滤镜、艺术照。我要的是换脸源图，不是单纯好看的头像。
```

### AI 判断源图是否合格命令

用途：把候选源图交给 AI，让它按换脸标准判断是否值得进入测试。

给 AI 的命令：

```text
请判断我提供的这张图片是否适合做实时单图换脸源脸图。不要只评价好不好看，要按换脸效果判断。

请按 0-5 分评分，并给出结论：
1. 正脸角度：是否正脸，左右/上下偏转是否小于 10 度。
2. 脸部完整度：额头、下巴、左右脸颊、脸部外轮廓是否完整。
3. 清晰度：眼睛、鼻子、嘴唇、脸颊边缘是否清楚，有无压缩、糊、噪点。
4. 光线：是否均匀，有无强阴影、过曝、偏色。
5. 遮挡：是否有眼镜、手、头发、麦克风、帽子遮挡关键区域。
6. 嘴部状态：嘴唇是否自然，是否闭嘴或轻微放松，是否夸张口红。
7. 美颜程度：是否过度磨皮、小脸、大眼、滤镜化。
8. 脸型匹配风险：脸宽、下巴、颧骨、额头是否可能和目标摄像头脸型冲突。
9. 边缘风险：是否可能导致脸周围虚浮、穿模、边缘糊。
10. 实时使用风险：动态说话、转头时是否容易一眼假。

请用表格输出：
评分项 / 分数 / 问题 / 会导致的换脸后果

最后给一个结论，只能三选一：
可直接测试
可测试但需记录缺陷
不建议作为主测试源图

如果不合格，请指出最该重新生成或替换的 3 个问题。
```

### OBS 当前已做优化

当前 OBS 只负责画面输出稳定和轻微均衡，不负责修复换脸贴合。

已做：

```text
OBS profile：DeepLiveCam
OBS scene collection：DeepLiveCam
输入源：窗口捕获 OBS Output:HighGUI class:python.exe
捕获方式：WGC
捕获客户端区域：true
捕获鼠标：false
OBS 画布：1280x720
OBS 输出：1280x720 / 30fps
颜色格式：NV12
颜色空间：709
颜色范围：Partial
DroidCamVirtualOutput AutoStart：true
WhatsApp 摄像头：DroidCam Video
```

OBS 当前画面位置：

```text
有效画面区域：970x546
位置：X=155, Y=86
缩放过滤：Lanczos
目的：让 WhatsApp 里画面比例别太大，保留头肩空间。
状态：当前稳定回退值
不要使用：大小 506x900、边界 970x545 这类竖向构图。它不是手机适配优化，而是在横屏链路里制造不稳定构图。
```

OBS 当前滤镜：

```text
01 Sharpen
类型：sharpness_filter
数值：0.18
作用：轻微锐化，补偿 WhatsApp/DroidCam 压缩后的软。
风险：过高会让边缘、皮肤纹理、换脸边界更假。

02 Color Stable
类型：color_filter
contrast：0.06
brightness：0
saturation：0.04
hue_shift：0
opacity：100
作用：轻微提升对比和饱和，让画面不灰。
风险：过高会让嘴唇、脸颊、边缘色差更明显。
```

OBS 可做但未尝试：

```text
轻微柔化皮肤：可能让整体更均衡，但会降低细节。
LUT/色彩预设：可能改善肤色，但容易让嘴唇和脸颊偏色。
更小画面区域，例如 900x506：可能让 WhatsApp 中脸更小更自然，但远端细节会更少。
1080p OBS 输出：可能更清晰，但 WhatsApp/DroidCam 是否接收不确定，可能增加延迟。
更强锐化 0.22-0.30：可能提高清晰感，但会放大边缘穿模。
降噪/磨皮插件：OBS 原生不擅长真实美颜，插件稳定性和延迟要单独验证。
```

铁律：OBS 调整只用于最终画面均衡，不能用 OBS 掩盖 DeepLiveCam 的脸型、mask、嘴部问题。

### DeepLiveCam 当前实时参数

当前日常启动入口：

```text
1_START_LOW_LATENCY.cmd
```

实际启动链路：

```text
source_faces\current_test_source.jpg
run.py
--execution-provider cuda
--execution-threads 2
--frame-processor face_swapper_hyperswap
--live-resizable
--live-fps-debug
--live-health-log
--similar-face-distance 1.5
--live-face-smooth 0.25
--live-face-fit-scale 1.06
--quality-preset low_latency
--live-obs-output-window
--no-live-virtualcam-output
--auto-live
--no-live-xseg-mask
```

当前 low_latency 实际策略：

```text
主模型：HyperSwap 256
Face Enhancer：None
GPEN512：关闭
XSeg：关闭
Poisson Blend：关闭
颜色校正：开启
颜色迁移强度：0.25
DeepLiveCam 锐化：0.18
face_mask_scale：0.45
face_mask_blur：23
face_fit_scale：1.06
live_face_smooth：0.25
similar_face_distance：1.5
插帧/平滑：开启
interpolation_weight：0.82
```

不同部位/问题对应参数：

| 部位/问题 | 当前值 | 可测范围 | 作用 | 风险 |
|---|---:|---:|---|---|
| 整张脸贴合大小 | face_fit_scale 1.06 | 1.04 / 1.06 / 1.08 / 1.10 | 控制换脸区域贴回目标脸的覆盖大小 | 过小露原脸边缘，过大脸像浮上去 |
| 脸部边缘范围 | face_mask_scale 0.45 | 0.43 / 0.45 / 0.47 / 0.50 | 控制椭圆 mask 覆盖脸周围的范围 | 过小边缘露底，过大压到头发/脸型 |
| 脸部边缘柔和 | face_mask_blur 23 | 19 / 23 / 31 / 39 | 控制边缘过渡软硬 | 过低硬边，过高脸周围发虚 |
| 动态防抖 | live_face_smooth 0.25 | 0.20 / 0.25 / 0.35 | 减少脸框抖动和漂移 | 过高会有延迟和粘滞感 |
| 人脸识别距离 | similar_face_distance 1.5 | 1.2 / 1.5 | 多脸或映射时控制匹配宽松度 | 单人场景影响较小，过严可能丢脸 |
| 嘴部动态 | mouth_mask 关闭，mouth_mask_size 0 | 10 / 20 / 30 起测 | 保留更多目标嘴部运动和牙齿区域 | 过大会出现嘴部身份断层 |
| 语义遮罩 | XSeg 关闭 | quality 模式再开 | 改善脸周围、头发、遮挡边缘 | 显著增加耗时，低配会掉帧 |
| 清晰增强 | Face Enhancer None | GPEN-256，再 GPEN-512 | 增强脸部细节 | 延迟大，动态贴合可能变差 |
| OBS 锐化 | 0.18 | 0.12 / 0.18 / 0.24 | 提升远端清晰感 | 放大边缘瑕疵 |
| OBS 饱和 | 0.04 | 0.00 / 0.04 / 0.08 | 改善灰和低饱和 | 嘴唇、脸颊可能过艳 |

### DeepLiveCam 质量档位对比

```text
low_latency：
poisson_blend=false
color_transfer_strength=0.25
sharpness=0.18
face_mask_scale=0.45
face_mask_blur=23
live_xseg_mask=false
face_fit_scale=1.06
interpolation_weight=0.82
适合：WhatsApp 实时测试、帧率优先、当前主方案。

balanced：
poisson_blend=false
color_transfer_strength=0.35
sharpness=0.25
face_mask_scale=0.47
face_mask_blur=31
live_xseg_mask=false
face_fit_scale=1.06
interpolation_weight=0.72
适合：低风险提升边缘和颜色，但仍要单变量测试。

high_quality：
poisson_blend=true
color_transfer_strength=0.45
sharpness=0.32
face_mask_scale=0.50
face_mask_blur=39
live_xseg_mask=true
face_xseg_mask_strength=0.90
face_xseg_mask_blur=15
face_xseg_mask_dilate=7
face_fit_scale=1.09
interpolation_weight=0.62
适合：新硬件质量验证，不作为当前默认。
```

### 调试顺序

当前不要同时改多个变量。

```text
1. 先固定源图、摄像头位置、灯光、WhatsApp 链路。
2. 只换源图，选出最真实的 1-2 张。
3. 只调 face_fit_scale，看脸是否浮、是否露边。
4. 固定 face_fit_scale 后，只调 face_mask_scale。
5. 固定 mask scale 后，只调 face_mask_blur。
6. 再测试 mouth_mask_size，重点看说话自然度。
7. 新硬件上再测试 XSeg 和 high_quality。
8. 最后才调 OBS 美化。
```

验收标准：

```text
本地 OBS Output 不能一眼假。
OBS 预览不能明显发灰、过锐或过艳。
WhatsApp 远端不能明显卡顿、掉脸、黑屏。
正脸说话时嘴不能像贴纸。
轻微转头时脸边缘不能明显露底。
任何一次优化都要记录：改了什么、FPS、边缘、嘴部、远端观感。
```

## 2026-09-02 嘴部微调与 AI 源图流程

### 当前稳定参考值

这组值作为当前可恢复基准，后面调坏了先恢复这里：

```text
启动入口：1_START_LOW_LATENCY.cmd
主模型：HyperSwap 256
Face Enhancer：None
XSeg：关闭
GPEN512：关闭
face_fit_scale：1.06
face_mask_scale：0.45
face_mask_blur：23
live_face_smooth：0.25
嘴部遮罩 Mouth Mask：0
OBS 锐化：0.18
OBS contrast：0.06
OBS saturation：0.04
OBS 输出：1280x720 / 30fps
WhatsApp 摄像头：DroidCam Video
```

### 嘴部参数在哪里调

手动位置：

```text
DeepLiveCam 主界面
-> 细节调整 Refinement
-> 嘴部遮罩 Mouth Mask 滑条
```

含义：

```text
嘴部遮罩 Mouth Mask = 0：完全使用换脸后的嘴部，身份感更强，但说话/露牙/撅嘴可能僵。
嘴部遮罩 Mouth Mask 增大：保留更多真实嘴部和牙齿动态，说话更自然，但身份相似度可能下降。
```

本轮只允许小范围测试：

```text
稳定参考：0
第一档：10
第二档：15
第三档：20
暂时不要超过 20
```

测试动作：

```text
正常说话 20 秒
露牙说话 10 秒
撅嘴 5 秒
轻微左右转头说话 10 秒
```

记录格式：

```text
嘴部遮罩 Mouth Mask：
FPS：
说话自然度：好/一般/差
露牙自然度：好/一般/差
撅嘴自然度：好/一般/差
嘴角断层：无/轻微/明显
身份相似度下降：无/轻微/明显
WhatsApp 远端观感：
结论：保留/放弃/继续微调
```

判断规则：

```text
如果 10 比 0 自然，且没有明显嘴角断层，10 作为候选。
如果 15 比 10 自然，且身份感没有明显下降，15 作为候选。
如果 20 出现嘴角断层或嘴不像源图，停止上调。
如果 0 仍然最好，不再调嘴部，转向源图和边缘。
```

### 和 ChatGPT 交流生成 AI 源图

第一轮对 ChatGPT 说：

```text
我在做实时单图换脸测试，所有参考素材都已获得授权。目标不是生成漂亮头像，而是生成适合换脸软件使用的标准源脸图。

我会上传几张同一人物的参考照片。请你先不要生成，先判断这些参考照片能不能作为生成依据，并按换脸源图标准评分。

请重点判断：
1. 身份特征是否稳定。
2. 脸型是否一致，是否存在明显美颜小脸或广角畸变。
3. 是否有正脸、无遮挡、光线均匀的参考。
4. 嘴唇颜色和嘴部状态是否自然。
5. 是否适合生成一张 1:1 标准正脸源图。

输出格式：
参考图编号 / 可用程度 / 问题 / 会影响换脸的哪里 / 是否建议用于生成

最后告诉我：
是否可以开始生成；
最适合参考哪几张；
生成时要避开什么问题。
```

ChatGPT 判断可生成后，再说：

```text
请基于我选定的参考图生成 3 张真实照片级标准正脸源图，用于实时单图换脸测试。

共同要求：
1. 保持同一人物身份特征，不改变年龄、人种、脸型核心特征。
2. 正脸，头部水平，眼睛看镜头，左右/上下偏转不超过 10 度。
3. 镜头与眼睛同高，不俯拍、不仰拍、无广角畸变。
4. 面部完整，额头、下巴、左右脸颊轮廓完整可见。
5. 无眼镜、无手、无头发遮挡眼睛、嘴巴、脸颊边缘。
6. 表情自然，闭嘴或轻微放松，不露牙，不撅嘴。
7. 嘴唇颜色自然，不要大红唇、紫色嘴唇、厚重妆容。
8. 光线均匀柔和，皮肤保留真实纹理，不要重度磨皮。
9. 背景干净，无文字、水印、贴纸。
10. 输出 1:1，1024x1024 或更高，真实相机人像。

三张分别偏向：
A 身份相似优先。
B 脸型贴合优先，脸宽、下巴、颧骨更自然。
C 换脸测试优先，轮廓、嘴部、光线最标准。
```

生成后再让 ChatGPT 自检：

```text
请按实时单图换脸源图标准重新检查你刚生成的 3 张图。

逐张评分：
正脸角度、脸型自然度、面部完整度、嘴部自然度、皮肤真实纹理、光线、遮挡、边缘风险、实时说话风险、整体推荐度。

最后只选 1 张最适合进入 DeepLiveCam 测试，并说明为什么。
```

## WhatsApp 手机比例双路线验证（2026-09-18）

### 结论先行

当前正式路线继续使用：

```text
DeepLiveCam -> OBS 1280x720/30 -> DroidCam Video -> WhatsApp Desktop
```

该路线已经验证可用，目标是稳定横屏通话，不再尝试把 DroidCam 0.2.2 强制改成 9:16。DroidCam Virtual Output 0.2.2 已移除实验性 9:16；开发者也确认 WhatsApp Desktop 会忽略竖向摄像头输出并按横向格式缩放。因此，单纯把 OBS 画布改成 720x1280 不能保证远端手机收到真正的手机竖屏画面。

唯一保留的竖屏候选路线：

```text
DeepLiveCam -> OBS Virtual Camera -> BlueStacks 5 -> WhatsApp Android -> 对方手机
```

BlueStacks 官方确认其最新版本能够读取 OBS Virtual Camera。该路线必须作为独立试验，不替换正式路线，直到通过下面全部验收。

参考依据：

```text
BlueStacks 官方 OBS Virtual Camera 指南：
https://support.bluestacks.com/hc/en-us/articles/10814798704653-How-to-use-OBS-Virtual-Camera-on-BlueStacks-5

DroidCam Virtual Output 官方资源和版本说明：
https://obsproject.com/forum/resources/droidcam-virtual-output.1580/

DroidCam 开发者关于 WhatsApp、9:16 和模拟器的说明：
https://obsproject.com/forum/threads/droidcam-virtual-output.158752/page-4
```

### 测试优先级和停止条件

#### A：正式横屏基线

入口：`1_START_LOW_LATENCY.cmd`

步骤：

```text
1. 确认 OBS Output 正在显示实时换脸画面。
2. WhatsApp Desktop 选择 DroidCam Video，通话稳定运行 2 分钟。
3. 运行 7_CAPTURE_ROUTE_RESULT.cmd，保持正常说话和轻微转头 30 秒。
4. 报告必须显示 ValidLiveWorkload: True；否则该样本自动作废。
```

记录：DeepLiveCam FPS、GPU 平均/峰值、显存、进程内存、OBS/DroidCam 状态以及远端手机视觉评分。

#### B1：模拟器摄像头兼容

备用入口：`6_START_MOBILE_FORMAT.cmd`

该入口使用独立 OBS profile/scene `DeepLiveCam-Mobile`。虚拟摄像头传输固定为桌面应用兼容性更高的 `1280x720/30fps`，画面中央保留 `405x720`（9:16）竖屏内容，左右留黑。它同时启动 DroidCam Virtual Output 和 OBS Virtual Camera：WhatsApp Desktop 选择 `DroidCam Video`，Telegram 或后续模拟器选择 `OBS Virtual Camera`。它不启动模拟器，也不修改换脸模型、源图、mask、face fit 或画质参数。重启电脑后再次运行该入口会自动重建手机格式配置。

该方案已验证能够恢复 WhatsApp/Telegram 摄像头连接，但手机端有效视频面积会因左右黑边而缩小，不能达到正常手机原生通话的占屏效果。因此只作为兼容备用方案，不作为最终手机比例方案。

新电脑若设备列表没有 `OBS Virtual Camera`，先运行一次 `8_INSTALL_OBS_VIRTUAL_CAMERA.cmd`，接受 Windows 管理员授权，然后完全退出并重新打开 Telegram。

手机格式采用确定性中心裁切：从 `1280x720` 原画面保留中央 `405x720`。虚拟摄像头仍传输标准 `1280x720`，避免 WhatsApp Desktop 和 Telegram 拒绝非常规的 `720x1280` 摄像头格式。人物不会被横向拉伸；若人物不在中央，应移动摄像头/坐姿，而不是修改缩放比例。

模拟器实验入口：`9_START_ANDROID_EMULATOR_TEST.cmd`

该入口使用完全独立的 `DeepLiveCam-Android` profile/scene，将 OBS Virtual Camera 原生设为 `720x1280/30fps`，关闭 DroidCam 输出，并启动 BlueStacks `Pie64` 实例。BlueStacks 固定为 `720x1280`、30fps、摄像头 `OBS Virtual Camera`。入口 1 的稳定横屏配置和入口 6 的备用配置均不被覆盖。

BlueStacks 不要求登录 Google 账号。若 Play 商店因宿主机 VPN/代理未被 Android NAT 正确继承而无法连接 Google 服务器，直接从 WhatsApp 官方地址 `https://www.whatsapp.com/android/current/WhatsApp.apk` 下载 APK，并通过 BlueStacks 的 `installApk` 命令侧载。2026-09-19 已验证官方 APK `com.whatsapp` 版本 `2.26.37.70` 安装成功。Google 登录问题不属于摄像头链路，不作为本项目阻塞项。

WhatsApp Android 仍必须完成账号认证才能发起通话。优先使用“关联为辅助设备/Link as companion device”，由用户本人使用主手机扫描二维码；不要在脚本、日志或测试文档中保存验证码、二维码、账号或聊天数据。WhatsApp Desktop 不参与模拟器路线，也不能把现有桌面登录状态直接共享给 Android 应用。

2026-09-19 预兼容测试：BlueStacks 5.22.265.1012 的 `Pie64`（Android 9、x86_64）成功安装并启动官方 WhatsApp `2.26.37.70`，但应用进入 `com.whatsapp.registration.app.EULA` 后在约 0.2-3 秒内退出并返回桌面。崩溃发生在登录前，与 OBS、摄像头、WhatsApp 账号和 Google 登录无关；该 Pie64 实例判定不兼容，不再用于账号测试。下一步最多只测试一次全新 Android 11 实例：先验证欢迎页稳定 60 秒和系统相机读取 OBS Virtual Camera，再决定是否使用独立测试账号。Android 11 若仍闪退，立即淘汰模拟器路线。

账号安全边界：模拟器验证不得使用重要主账号起步，不得使用修改版 WhatsApp、自动注册、自动验证码、批量消息或自动拨号。只使用官方 APK、稳定网络出口、人工关联和知情测试联系人。换脸内容必须获得相关人员授权并避免冒充或误导；否则即使技术链路正常，也可能违反 WhatsApp 使用条款并带来账号限制风险。

首次只验证：

```text
BlueStacks 5 -> 设置 -> 设备 -> 摄像头 -> OBS Virtual Camera
Android 相机预览能连续看到 OBS 输出 60 秒
无黑屏、无静止帧、方向正确
```

任一项失败即停止，不登录 WhatsApp，不做后续性能和视觉测试。

#### B2：WhatsApp Android 真实通话

只有 B1 通过后执行。使用 WhatsApp Android 向同一台对方手机发起通话，运行 2 分钟后执行 `7_CAPTURE_ROUTE_RESULT.cmd`。

视觉判断由用户完成，只判断远端手机实际收到的结果：

```text
1. 是否和真实手机发起的视频一样是手机竖向构图。
2. 是否仍被强制放进横屏框。
3. 是否有明显上下或左右黑边。
4. 清晰度、动作流畅度、换脸真实感各打 1-5 分。
```

### 验收标准

视觉必须同时满足：

```text
远端手机显示为真正手机比例，而不是横屏里塞一张竖图。
人物尺寸和正常手机视频接近。
没有明显黑边、裁头、强制横屏或异常旋转。
动态换脸真实感不低于正式横屏路线。
```

性能以 A 基线为参照：

```text
DeepLiveCam 平均 FPS 下降不超过 10%。
GPU 平均占用增加不超过 15 个百分点。
模拟器新增内存不超过 2.5 GB。
口型和头部动作的主观额外延迟不超过约 250 ms。
连续 10 分钟无黑屏、冻结、虚拟摄像头断流。
```

最终决策：

```text
视觉不是真竖屏：直接淘汰模拟器路线。
视觉通过但 FPS/延迟不通过：保留为非实时备选，不进入生产。
视觉和性能全部通过：模拟器路线成为“手机比例模式”，Desktop+DroidCam 仍保留为稳定横屏模式。
```

### 测试边界

本轮只改变输出路线，不改变换脸效果参数。禁止同时调整源图、mouth mask、XSeg、face_fit_scale、enhancer、OBS 美颜或摄像头分辨率。这样出现差异时，才能确定来自 Desktop/DroidCam 或 Android 模拟器路线，而不是换脸算法。

