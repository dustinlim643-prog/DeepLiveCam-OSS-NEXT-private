# 实时单图换脸项目总文档

更新时间：2026-08-17

## 一句话目标

本项目只做一个核心目标：**实时单图换脸**。

当前不做 DFM 训练，不依赖 DeepFaceLive，不绕授权，不复制私有 Pro 编译模块。目标是在无授权限制的开源 Deep-Live-Cam 基础上，把单张授权源脸图片实时换到摄像头画面里，再稳定输出给 WhatsApp。

## 当前唯一主目录

```text
D:\DeepLiveCam2.7-Pro-0730_OSS_READY
```

旧目录、旧 DeepFaceLive、旧授权版目录只作为历史参考，不作为当前主方案。

## 当前唯一启动入口

用户只需要看根目录这 3 个文件：

```text
1_START.cmd              启动质量模式
1_START_LOW_LATENCY.cmd  启动流畅模式
2_STOP.cmd               停止本项目相关进程
3_CHECK.cmd              自检配置
```

不要再使用旧的中文 bat、DeepFaceLive 入口、单独 OBS 入口或 Pro 授权版入口。

质量模式用于看真实度，流畅模式用于判断卡顿是否来自 XSeg/high_quality/当前硬件。两套模式互不覆盖，避免来回调乱。

## 当前最终链路

```text
DeepLiveCam
  -> Live / 直播
  -> OBS Output 窗口
  -> OBS 捕获 DeepLiveCam OBS Output
  -> DroidCam Virtual Output 插件
  -> Windows 摄像头设备 DroidCam Video
  -> WhatsApp Desktop 选择 DroidCam Video
```

关键结论：

- WhatsApp 不再直接选择 `OBS Virtual Camera`。
- WhatsApp 必须选择 `DroidCam Video`。
- OBS 仍然保留，用来捕获、缩放、锐化、调色和输出给 DroidCam。
- DeepLiveCam 自己的虚拟摄像头输出已禁用，避免和 OBS/DroidCam 抢设备。

## 当前默认启动配置

启动脚本：`tools\start_deeplivecam_obs.ps1`

当前默认源脸：

```text
source_faces\saokai11.jpg
```

当前 DeepLiveCam 参数：

```text
--source source_faces\saokai11.jpg
--execution-provider cuda
--execution-threads 2
--frame-processor face_swapper_hyperswap
--live-resizable
--live-fps-debug
--similar-face-distance 1.5
--live-face-smooth 0.35
--live-face-fit-scale 1.06
--quality-preset high_quality
--live-xseg-mask
--live-obs-output-window
--no-live-virtualcam-output
--auto-live
-l zh
```

含义：

- `face_swapper_hyperswap`：当前默认使用 256 单图换脸模型路线。
- `high_quality`：质量优先预设。
- `--live-xseg-mask`：启用 XSeg 语义脸部 mask，优先减少边缘、头发、脸轮廓穿帮。
- `--live-face-smooth 0.35`：降低实时抖动。
- `--live-face-fit-scale 1.06`：让贴回区域略放大，减少目标脸比源脸大时露底。
- `--no-live-virtualcam-output`：不让 DeepLiveCam 直接输出虚拟摄像头，统一走 OBS。

## 当前测试模式

质量模式：

```text
入口：1_START.cmd
HyperSwap 256
XSeg 开
quality preset：high_quality
Face Enhancer：关
```

流畅模式：

```text
入口：1_START_LOW_LATENCY.cmd
HyperSwap 256
XSeg 关
quality preset：low_latency
Face Enhancer：关
```

判断规则：

```text
质量模式看真实度。
流畅模式看当前硬件下是否能显著改善卡顿。
如果流畅模式明显更顺但边缘变差，说明等待更强硬件后应回到质量模式。
如果流畅模式仍卡，说明当前电脑已经不适合判断最终流畅度。
```

## 当前 OBS / WhatsApp 配置

OBS：

```text
场景：DeepLiveCam
源：DeepLiveCam OBS Output
捕获窗口：OBS Output:HighGUI class:python.exe
画布：1280x720
FPS：30
```

WhatsApp：

```text
摄像头：DroidCam Video
不要选：OBS Virtual Camera
```

当前 WhatsApp 安全比例：

```text
OBS 画布：1280x720
源画面区域：970x546
位置 X：155
位置 Y：86
```

如果画面仍然太大：

```text
源画面区域：900x506
位置 X：190
位置 Y：107
```

## 摄像头策略

当前同意并固定的方向：

```text
摄像头输入：1920x1080 / 30fps
OBS 输出：先保持 1280x720 / 30fps
WhatsApp：走 DroidCam Video
不建议默认用 4K
```

原因：

- WhatsApp 最终会压缩，4K 不会让对方看到真正 4K。
- 4K 会增加 DeepLiveCam、OBS、DroidCam、WhatsApp 的整体压力。
- 当前项目优先真实、稳定、低延迟，1080p 输入比 4K 更合理。
- 如果 4K 导致掉帧，最终观感会比干净的 1080p 更差。

## 当前已经做到什么

### 已经跑通

- 开源无授权限制项目作为主方案。
- DeepLiveCam 实时摄像头换脸。
- OBS 捕获最终换脸输出窗口，不捕获 cmd，不捕获原始摄像头。
- DroidCam Video 被 WhatsApp Desktop 正常识别。
- WhatsApp 视频通话能选择 DroidCam Video 并显示画面。
- OBS Virtual Camera 与 WhatsApp 不兼容的问题已绕开。

### 已经接入的质量模块

```text
HyperSwap 256
XSeg mask
Poisson Blend
mouth mask
单人目标追踪
bbox/keypoint 平滑
face fit scale
GPEN-256 / GPEN-512 可选
GFPGAN 可选
OBS 锐化 / 颜色稳定
```

### 当前模型文件

```text
inswapper_128.onnx
inswapper_128_fp16.onnx
hyperswap_1a_256.onnx
hyperswap_1b_256.onnx
hyperswap_1c_256.onnx
GPEN-BFR-256.onnx
GPEN-BFR-512.onnx
GFPGANv1.4.onnx
GFPGANv1.4.fp16.onnx
gfpgan-1024.onnx
xseg.onnx
xseg_cuda_v1.onnx
insightface\buffalo_l
```

## 当前还缺什么

这些是“真实感”差距，不是简单 FPS 问题：

1. **更强的遮挡保护**
   手、眼镜、头发、麦克风经过脸部时，当前 XSeg 只能减轻穿帮，不能保证完全保护。

2. **更强的脸型约束**
   如果源脸和目标脸脸宽、下巴、额头比例差太多，当前只能靠 `face fit scale` 和 mask 缓解，不能彻底改变脸型结构。

3. **更强的动态跟踪**
   快速转头、低头、侧脸时，当前平滑能减少抖动，但无法保证每帧都完美贴合。

4. **更智能的局部保护**
   眼睛、牙齿、嘴唇、下巴边缘还没有足够精细的独立保护策略。

5. **更强的生成模型**
   HyperSwap 256 比 inswapper_128 上限更高，但仍是单图实时方案，不等于训练人物专属模型。

## 不要误解显卡升级

4080 / 4080S / 5070Ti 能解决的是：

- 更高 FPS。
- 更低延迟。
- 更稳定开启 XSeg、GPEN-256、GPEN-512。
- 减少因为性能不足导致的画面波动。
- 让高质量参数有实际测试空间。

显卡不能直接解决的是：

- 源脸图片不合适。
- 源脸和目标脸型差太大。
- 嘴唇颜色/妆容本身异常。
- 单图模型身份保持能力不足。
- WhatsApp 自身压缩导致对方画面变糊。

所以硬件升级后，测试重点不是“FPS 变高了没有”，而是看能否稳定开启高质量模块后，真实感是否明显改善。

## 单图源脸图片质量标准

这是本项目最重要的素材标准。源图不合格，配置再漂亮也会拉跨。

### A 级，可优先测试

- 正脸，左右转头小于 10 度。
- 低头/仰头很轻，眼睛、鼻子、嘴巴位置自然。
- 脸部宽度至少 300px，越大越好。
- 双眼之间距离至少 120px，越大越好。
- 眼睛、鼻翼、嘴唇边缘清晰。
- 光线均匀，不背光，不过曝，不一边脸黑一边脸亮。
- 没有头发遮眼、手遮脸、墨镜、口罩、强反光眼镜。
- 表情自然，闭嘴或轻微微笑。
- 皮肤和嘴唇颜色自然，没有重滤镜、紫色嘴唇、过度美颜。
- 原图或高质量图片，不是多次压缩后的聊天软件小图。
- 脸型和目标摄像头里的脸尽量接近，尤其是脸宽、下巴、额头比例。

### B 级，可以测试但要记录问题

- 轻微侧脸，左右转头 10-20 度。
- 有轻微美颜或轻微磨皮。
- 戴普通眼镜但无强反光。
- 光线略不均匀但五官清楚。
- 脸部够大但截图压缩较明显。

### C 级，不建议作为主测试图

- 大角度侧脸。
- 脸太小、截图糊、视频运动模糊。
- 强滤镜、强磨皮、AI 修复过度。
- 嘴唇颜色异常、妆容过重。
- 头发遮住眼睛或脸边缘。
- 表情夸张、张嘴、大笑。
- 低清、过曝、背光、阴影很重。

## 真实感验收标准

不要只看“清不清楚”，要按下面维度评分。

每项 1-5 分：

```text
1 = 无法使用
2 = 明显假
3 = 可测试但破绽明显
4 = 基本可用
5 = 接近真实
```

### 1. 身份相似度

看起来是否像源图人物。重点看眼型、鼻子、嘴唇、脸部整体气质。

### 2. 脸型贴合

看脸宽、下巴、额头、脸颊是否贴在目标脸上，不要像一张脸皮浮在上面。

### 3. 边缘融合

看额头、太阳穴、脸颊、下巴、耳侧是否露底、糊边、硬边。

### 4. 五官稳定

转头、眨眼、说话时眼睛、鼻子、嘴巴是否漂移。

### 5. 嘴部真实

说话时嘴唇颜色、嘴形、牙齿区域是否自然。

### 6. 遮挡表现

手、眼镜、头发靠近脸部时是否穿模。

### 7. 清晰度

看眼睛、鼻翼、嘴唇边缘，不只看皮肤是否光滑。

### 8. 颜色一致

源脸区域和摄像头原画面肤色是否一致，不偏紫、不偏灰、不像贴纸。

### 9. 动态稳定

正常说话、轻微转头 5-10 分钟，画面是否持续稳定。

### 10. 远端效果

WhatsApp 对方手机看到的画面是否仍可接受。最终以对方看到的画面为准。

## 测试流程

每次测试只改一个变量。不要同时换图、换模型、换 enhancer、换 OBS 分辨率，否则无法判断原因。

### 第 1 步：固定环境

```text
摄像头：1920x1080 / 30fps
OBS：1280x720 / 30fps
WhatsApp：DroidCam Video
灯光：固定
坐姿和脸到摄像头距离：固定
```

### 第 2 步：源图测试

固定所有参数，只换源脸图片。

记录：

```text
源图文件名
图片等级：A/B/C
身份相似度
脸型贴合
边缘融合
嘴部真实
颜色一致
备注
```

### 第 3 步：模型/处理器测试

同一张源图下对比：

```text
HyperSwap 256
inswapper_128
HyperSwap 256 + XSeg
HyperSwap 256 + XSeg + GPEN-256
HyperSwap 256 + XSeg + GPEN-512
```

当前重点不是 FPS，而是看哪个组合最真实。

### 第 4 步：动态测试

每个候选配置必须做：

```text
正脸静止 30 秒
正常说话 1 分钟
左右轻微转头
低头/抬头
手靠近脸部
戴眼镜/不戴眼镜，如果实际会用
WhatsApp 对方手机观看
```

### 第 5 步：记录结论

只保留有价值的结论：

```text
哪张图最好
哪组参数最好
最大破绽是什么
这个破绽属于源图问题、模型问题、mask 问题、硬件性能问题，还是 WhatsApp 压缩问题
下一次只改什么
```

## 当前优化方向排序

优先级从高到低：

1. 严格筛选 A 级源图。
2. 固定 1080p 摄像头输入和稳定灯光。
3. 继续以 HyperSwap 256 为主模型测试。
4. 保持 XSeg mask 作为质量优先方案。
5. 在新硬件上重新测试 GPEN-256 / GPEN-512。
6. 优化 mouth mask、眼部/嘴部局部保护。
7. 对比 deeplivecam.net 官方新版，但先做同素材、同灯光、同 WhatsApp 输出对比。
8. 如果单图方案仍达不到目标，再重新讨论是否进入训练或更高阶模型路线。

## 当前不做的事

- 不训练 DFM。
- 不继续 DeepFaceLive 方案。
- 不绕授权。
- 不复制 Pro 私有模块。
- 不盲目改 4K。
- 不只看本地 OBS 预览，必须看 WhatsApp 远端实际效果。

## 外部方案对比原则

未来测试 `https://deeplivecam.net/index.php` 或官方新版时，必须按同一标准比较：

```text
同一张源脸图
同一台摄像头
同一光线
同一 OBS/DroidCam/WhatsApp 输出链路
同一动作测试
同一评分表
```

如果新版只是 FPS 高，但边缘、嘴部、脸型没有改善，不算成功。

如果新版让遮挡、脸型、嘴部、动态稳定明显更好，即使配置复杂，也值得迁移。

## 硬件采购结论暂定

当前偏向：

```text
优先：RTX 4080 Super / RTX 4080
备选：RTX 5070Ti
不建议长期主力：RTX 4070Ti 12GB
```

原因：

- 4080/4080S 的 16GB 显存够用，软件生态成熟，实时链路更稳。
- 5070Ti 有新架构潜力，但需要更严格匹配 CUDA / ONNX Runtime / PyTorch。
- 对当前目标来说，稳定开启高质量模块比纸面 AI TOPS 更重要。

最终采购前仍要看价格。如果 5070Ti 价格明显更好，也可以作为新平台方案，但要接受环境调试成本。

## 相关历史文档

这些文件保留为历史记录，但以后优先看本文档：

```text
WHATSAPP_DROIDCAM方案记录.zh-CN.md
实时换脸优化记录_2026-08-15.md
FEATURE_MIGRATION_PLAN.zh-CN.md
项目需求_配置_测试记录.md
使用说明.txt
FINAL_READINESS_REPORT.zh-CN.md
```

## 下一步

下一步不是继续盲目改参数，而是建立一套素材和配置对照测试：

1. 从授权素材里挑 5-10 张 A/B 级源脸图。
2. 每张图用当前默认配置跑同一套动作。
3. 按 10 项真实感标准打分。
4. 选出最接近真实的 1-2 张图。
5. 再在这 1-2 张图上测试 GPEN、mask、OBS 滤镜和新版软件。

只有这样，才能判断“是软件不行、模型不行、图不行，还是硬件不行”。
