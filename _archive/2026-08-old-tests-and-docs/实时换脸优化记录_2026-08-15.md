# 实时换脸优化记录 2026-08-15

## 当前主方案

项目目录：

`D:\DeepLiveCam2.7-Pro-0730_OSS_READY`

用户只使用根目录 3 个入口：

- `1_START.cmd`：启动 DeepLiveCam UI + OBS + OBS Virtual Camera
- `2_STOP.cmd`：停止本项目相关 DeepLiveCam/OBS/watcher 进程
- `3_CHECK.cmd`：自检当前配置

## 已加入的开源 Pro-like 保护

这些是用开源代码和本项目现有能力逼近 Pro 的效果，不是复制 Pro 私有编译模块：

1. 单人目标锁定：优先跟随上一帧同一个人脸，避免旁边人进入时误换。
2. 每帧检测：减少快速转头时的旧框滞后。
3. bbox/keypoint 平滑：降低脸框和关键点小幅抖动。
4. 贴合缩放：默认 `--live-face-fit-scale 1.06`，减轻目标脸比源图脸大时的边缘露底。
5. HyperSwap 256：默认使用 `face_swapper_hyperswap`，比 `inswapper_128` 的细节上限更高。
6. OBS watcher：等待 `Live Preview` 出现后自动刷新 OBS 捕获，减少 OBS 黑屏/捕获旧窗口问题。

## 当前默认启动参数

```text
--execution-provider cuda
--execution-threads 2
--frame-processor face_swapper_hyperswap
--live-resizable
--live-fps-debug
--similar-face-distance 1.5
--live-face-smooth 0.35
--live-face-fit-scale 1.06
--quality-preset balanced
-l zh
```

默认不启用 GPEN。原因：当前电脑无增强约 10 FPS，GPEN-256 约 7 FPS，GPEN-512 约 3 FPS。先保证主换脸和遮罩质量，再根据硬件叠加增强。

## XSeg 遮挡/语义 mask 评估

本项目已有：

- `models\xseg.onnx`
- `models\xseg_cuda_v1.onnx`

实测情况：

- 模型能加载。
- 当前环境只能走 CPU，不能走 CUDA。
- 空输入平均约 `103ms/次`。

当前调整：

用户要求先追求效果质量，帧率后续通过硬件解决。因此已经把 XSeg 接入 HyperSwap 贴回阶段，并在 `1_START.cmd` 默认启动链路中启用。

实现方式：

- HyperSwap 先生成 256x256 换脸结果。
- XSeg 对同一张 256x256 aligned crop 生成语义脸部 mask。
- 贴回时使用 `椭圆软 mask + XSeg mask` 融合，减少脸外背景、头发边缘、脸轮廓区域被错误覆盖。
- XSeg 只作为边缘/区域保护，不改变身份模型本身。

限制：

XSeg 当前仍只走 CPU，画质更稳但会掉帧。后续换高性能电脑后再评估是否能长期默认开启。

## 现在测试方向

1. 先用默认 `HyperSwap 256 + XSeg mask` 测试：确认贴合、转头、边缘。
2. 如果清晰度还不够，再手动选择 `GPEN-256` 看清晰度提升是否值得。
3. 不建议当前机器实时开 `GPEN-512`，除非只看静态效果。
4. 单图换脸最影响真实感的是源图质量：角度接近、脸型接近、清晰、自然光、无遮挡、不要过度美颜。
5. 如果主要问题是边缘穿模，优先换更接近目标脸型/角度的授权源图；软件侧下一步再做 XSeg/遮挡保护试验。
