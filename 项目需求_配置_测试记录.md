# DeepLiveCam 实时换脸项目记录

## 当前目标

- 使用无授权限制的开源 Deep-Live-Cam 代码。
- 摄像头实时换脸后输出到独立窗口。
- OBS 捕获最终换脸窗口，不捕获 cmd、不捕获原始摄像头。
- Telegram/其他视频软件选择 OBS Virtual Camera。
- 当前优先目标是稳定跑通、低延迟、画面尽量真实。
- 所有启动入口集中在本项目目录，不再依赖 `E:\AI\DeepFaceLive`。
- OBS 与 OBS 配置也放入本项目目录，尽量支持整包迁移到生产电脑。

## 最终项目位置

```text
D:\DeepLiveCam2.7-Pro-0730_OSS_READY
```

## 唯一推荐入口

启动：

```text
D:\DeepLiveCam2.7-Pro-0730_OSS_READY\启动_DeepLiveCam_OBS.bat
```

停止：

```text
D:\DeepLiveCam2.7-Pro-0730_OSS_READY\停止_DeepLiveCam_OBS.bat
```

生产环境检查：

```text
D:\DeepLiveCam2.7-Pro-0730_OSS_READY\生产环境检查.bat
```

不要再使用旧的 DeepFaceLive 启动方式。

## 当前配置

| 项目 | 当前值 |
| --- | --- |
| 源图片 | 在 DeepLiveCam 原版 UI 中点击 `Select a face` 手动选择 |
| 摄像头 | `0` |
| 输出窗口 | `DeepLiveCam output` |
| 分辨率 | `1280x720` |
| 帧率 | `30 FPS` |
| sharpen | `0.35` |
| contrast | `1.03` |
| DeepLiveCam 日志 | `D:\DeepLiveCam2.7-Pro-0730_OSS_READY\logs\live_output.log` |
| 操作记录 | `D:\DeepLiveCam2.7-Pro-0730_OSS_READY\logs\operation_log.txt` |
| OBS 程序 | `D:\DeepLiveCam2.7-Pro-0730_OSS_READY\obs-studio\bin\64bit\obs64.exe` |
| OBS 捕获目标 | `Live Preview` |
| 根目录用户入口 | 只保留 `启动_DeepLiveCam_OBS.bat`、`停止_DeepLiveCam_OBS.bat`、`更新后自检验证.bat` |

## OBS 要求

- 启动脚本优先使用项目内便携 OBS：`obs-studio\bin\64bit\obs64.exe`。
- OBS 使用便携模式：配置保存在 `obs-studio\config\obs-studio`。
- OBS 场景应捕获 DeepLiveCam 点击 `Live` 后弹出的 `Live Preview` 窗口。
- OBS 虚拟摄像头必须启动。
- Telegram/视频软件里选择 `OBS Virtual Camera`。

## 生产环境迁移要求

理论上可以把整个文件夹复制到生产电脑后运行：

```text
启动_DeepLiveCam_OBS.bat
```

但生产电脑必须满足：

- Windows 系统。
- NVIDIA 显卡和可用的 NVIDIA 驱动。
- 摄像头能被系统识别，默认摄像头索引为 `0`，否则需要改启动脚本里的 `--camera`。
- 视频软件能看到 `OBS Virtual Camera`。
- 如果新电脑第一次看不到 `OBS Virtual Camera`，需要在该电脑安装/注册 OBS 虚拟摄像头组件，或者安装一次 OBS Studio。
- 项目路径可以变，不要求必须是 D 盘；启动脚本使用相对路径。

## 素材选择要求

- 必须是授权素材。
- 优先使用清晰正脸、自然光、无遮挡、脸部不要太小的图片。
- 眼镜、强滤镜、过曝、低清晰度、大角度侧脸都会降低真实感。
- 当前源图片只是测试图，真实效果上限主要受源图质量、目标摄像头光线、电脑性能影响。

## Pro 参照与当前优化依据

参照资料：<https://aixplore.cc/posts/deep-live-cam-2-7-pro-face-swap/>

从 Pro 文档和本地 `D:\DeepLiveCam2.7-Pro-0730` 对照得到的结论：

- Pro 的画面增强核心不是 OBS，而是更高清的人脸模型、GFPGAN/GPEN 增强、直播保护、遮挡保护、脸型修改的组合。
- 当前 OSS_READY 已接入 HyperSwap 256、GPEN-256、1280x720 Live Preview 捕获、OBS 锐化/颜色稳定、相似度阈值和 mask 参数。
- 当前仍与 Pro 有差距：DFM、遮挡保护、脸型修改还需要后续以开源实现继续补齐。
- 因此后续优化必须按同一素材、同一摄像头、同一光线，对比 `GPEN-256 / GPEN-512 / GFPGAN / 无增强` 的实际清晰度和 FPS，不能只凭感觉改参数。

## 测试记录

| 时间 | 操作 | 结果 | 下一步 |
| --- | --- | --- | --- |
| 2026-08-11 20:30 | 统一项目入口，清理旧 DeepFaceLive 启动依赖 | 已完成 | 用统一 bat 启动测试 |
| 2026-08-11 20:33 | 使用 `启动_DeepLiveCam_OBS.bat` 启动 OBS 和 DeepLiveCam | 已跑通：OBS 进程正常，DeepLiveCam 输出窗口正常，帧数持续增长 | 保持统一入口 |
| 2026-08-11 20:33 | 检查 OBS 日志 | 已确认：OBS 捕获 `DeepLiveCam output`，Virtual Camera 已启动 | Telegram 选择 OBS Virtual Camera 测试 |
| 2026-08-11 20:33 | 检查 GPU | RTX 3060 Ti 正常工作，测试时约 7% GPU、1580 MiB 显存 | 后续按画质/延迟继续调参 |
| 2026-08-11 20:42 | 复制 OBS 到项目内并启用便携模式 | 已完成：`obs-studio` 已放入项目目录，创建 `portable_mode.txt`，复制当前 OBS 配置 | 用内置 OBS 启动测试 |
| 2026-08-11 20:44 | 使用项目内便携 OBS 启动 | 已跑通：OBS 路径为项目内 `obs-studio\bin\64bit\obs64.exe`，标题显示便携模式；DeepLiveCam 输出正常 | 生产电脑先跑 `生产环境检查.bat` |
| 2026-08-11 20:45 | 检查 OBS Virtual Camera | 已确认：DirectShow 设备列表可看到 `OBS Virtual Camera` | 检查脚本改为 DirectShow 检测 |
| 2026-08-11 20:50 | 更新傻瓜式使用流程 | 已写入 `使用说明.txt`，包含启动、停止、Telegram、换电脑、故障处理 | 按说明链路重新测试 |
| 2026-08-11 20:51 | 修复 OBS 启动安全提示 | 启动/停止脚本会清理便携 OBS 的 `.sentinel` 异常退出标记 | 重新测试完整链路 |
| 2026-08-11 20:52 | 按说明链路测试：停止、生产检查、启动 | 链路已通：生产检查全 OK；OBS 从项目内便携版启动；OBS 场景为 `DeepLiveCam`；DeepLiveCam 输出窗口出现；OBS 捕获目标为 `DeepLiveCam output` | 进入 Telegram 选择 `OBS Virtual Camera` 做对端画面确认 |
| 2026-08-11 20:52 | 检查流畅性 | DeepLiveCam 日志帧数持续增长，GPU 约 35%、显存约 1497 MiB；但后段帧数增长偏慢，当前不能算非常流畅 | 后续如卡顿，优先调低分辨率或优化检测频率 |
| 2026-08-11 20:56 | 补齐操作入口 | 已新增 `选择换脸图片.bat` 和 `打开DeepLiveCam原版操作界面.bat`；生产模式读取 `source_faces\current_source.jpg` | 用户需要换图时先选图再重启生产链路 |
| 2026-08-11 21:00 | 验证当前源图链路 | 已确认启动日志读取 `D:\DeepLiveCam2.7-Pro-0730_OSS_READY\source_faces\current_source.jpg`；OBS 与 DeepLiveCam output 均正常运行 | 后续换图统一用 `选择换脸图片.bat` |
| 2026-08-11 21:08 | 纠正为原版 DeepLiveCam UI 工作流 | 已修复 `run.py` 的 ffmpeg 路径；主入口改为打开原版 UI + OBS；OBS 捕获目标改为 `Live Preview`；删除误导的选择图片/单独 UI 入口 | 用户在 DeepLiveCam UI 中选择源脸和摄像头后点击 Live |
| 2026-08-11 21:10 | 切换中文 UI | 项目存在 `locales\zh.json`，主启动脚本已添加 `-l zh`；文档已改为中文按钮说明并保留英文兜底 | 重新启动后验证中文界面 |
| 2026-08-14 21:20 | 清理根目录启动入口 | 根目录只保留启动、停止、自检 3 个 bat；旧低延迟、旧 run-cuda/run-directml、手动修复 OBS、生产检查入口已移除 | 用户只需要按 3 个入口操作 |
| 2026-08-14 21:20 | 对照 Pro 文档分析画质 | 明确 Pro 主要依靠 256 换脸模型、GFPGAN/GPEN、直播保护、遮挡保护、脸型修改；当前已接 GPEN-256 和 OBS 轻增强，但主模型仍是 128 | 下一步按固定素材对比增强器/FPS |
| 2026-08-15 00:00 | 接入 HyperSwap 256 单人实时处理器 | 按 FaceFusion 开源实现参数接入 `hyperswap_1a_256.onnx`：`arcface_128` 模板、256 crop、source/target 输入、输出贴回；启动脚本默认使用 `face_swapper_hyperswap + GPEN-256` | 自检和实际 Live Preview/Telegram 验证 |

## 已知注意事项

- 当前 OBS 版本是 32.2.1，`--disable-shutdown-check` 在 32.x 中已不再可靠；不要强制结束 OBS，否则下一次可能出现“检测到 OBS Studio 崩溃/安全模式”提示。
- 已把停止脚本改成优先正常关闭 OBS，减少这个提示。
- 如果已经出现提示，选择正常启动即可；这不代表 DeepLiveCam 换脸链路失败。
